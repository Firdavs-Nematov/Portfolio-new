let options = {
    strings: ["Front End Developer", "Back end Developer"],
    typeSpeed: 80,
    backSpeed:80,
    loop: true,
    loopCount: Infinity

};

let typed = new Typed('.element', options);